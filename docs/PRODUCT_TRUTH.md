# Product Truth — v7 (2026-08-08)

The single versioned inventory of what this build actually is. `CLAUDE.md`, `AGENTS.md`,
`README.md`, `CHECKLIST.md`, and `docs/PLAYTEST.md` are all reconciled against this file.
No claim appears here without code, generator output, or a recorded test behind it.

Bump the version and the date whenever a row changes. If a doc and this file disagree,
this file is wrong until proven otherwise — fix it first, then the doc.

## How each number was obtained

| Command | Output used |
|---|---|
| `python3 scripts/validate_gym.py` | station / variant / instance counts, build hash |
| `ls src/ServerScriptService/Core` | server system count |
| `ls src/StarterPlayer/StarterPlayerScripts/Controllers` | client controller count |
| `grep 'Id = "' src/ReplicatedStorage/Modules/ZoneConfig.luau` | zone count |
| source read | control bindings, combat behavior, product ids |
| live Studio boot (2026-08-08) | ignited system counts, live spot status |

## Inventory

| Claim | Truth in this build | Evidence |
|---|---|---|
| World size | **35 stations—7 tiers × 5 muscles**, 2384 instances / 2184 BaseParts, build `4c572a6de1e6`; 15 original machine and pose definitions remain configured | `scripts/validate_gym.py` |
| World source | generated by `scripts/build_gym.py` into `src/Workspace/Gym/*.model.json` | repo layout |
| Zones | 11 configured themes; the first 7 are active at exact location multipliers x1, x2, x4, x8, x16, x32, x64. Void/Solar/Nebula/Ascendant are dormant future rows | `ZoneConfig.luau`, `scripts/validate_gym.py` |
| Server systems | 27 entries under `Core/` — **26 ignite** (`_Migrations` is a helper, skipped by the `_` rule) | `[Loader/Server] Ignited 26 systems` |
| Client controllers | 20 | `[Loader/Client] Ignited 20 systems` |
| Character | **uniform for everyone** — `LoadCharacterAppearance` off, clothing/accessories stripped, one skin tone, shorts painted onto the R15 UpperLegs | `AppearanceService.luau`, `default.project.json` |
| Stats | 5 — Arms (damage), Chest (max HP), Back (durability), Core (retaliation knockback), Legs (run + flight speed) | `StatConfig.luau`, Phase 14 |
| Abilities | **1 — Punch only.** Slam and Dash are *deferred*, not shipped | `CombatService/Abilities/` |
| Training start | hold **E** at a station prompt; server mounts, locks, and poses the player; reps tick automatically | `TrainingService.luau` |
| Training stop | hold **E** again, or press **Space** / jump | `TrainingService.luau`, `Net` `StopTraining` |
| Training interruption | **kill-only.** A hit deals damage and grounds the victim but does *not* dismount them; only death ends the set | `CombatService/init.luau` |
| Death cost | respawn timer, combo reset, in-flight token tick forfeited. No stat, cash, or token loss | `CHECKLIST.md` #33 |
| Flight | toggle **Q** or **double-tap Space**, ascend **Space**, descend **LeftShift** — **keyboard only**, no touch or gamepad binding | `MovementConfig.luau`, `FlightController.luau` |
| Flight steering | follows the camera's full look direction, pitch included; the rig is held upright and facing its heading by an `AlignOrientation` | `FlightController:_step` |
| Flight availability | available from spawn | commit `895d0e3` |
| Leaderboards | 2 global boards — Power and Kills | `LeaderboardService.luau` |
| Menu tabs | 7 — Map, **Train**, Upgrades, Quests, Shop, Top, Settings | `MenuController.luau` |
| Persistent navigation | one right-side **MENU** button, a bottom five-stat training dock, and a persistent training-first **MAIN GOAL** card; pressing a stat opens its Train view | `MenuBarController.luau`, `HudController.luau`, `MenuController:OpenTrain` |
| Finding a machine | the **Train** tab lists seven locations for the chosen muscle, usable first and weakest→strongest, with authored place names and live free/busy counts | `MenuController:_renderTrain`, `TrainingService:GetSpotStatus` |
| Teleporting | **Fast Travel gamepass only.** The plaza rule is gone. Without the pass a destination becomes a tracked waypoint — beacon plus off-screen arrow — and the player walks or flies | `TravelService:CanTravelFrom`, `WaypointController` |
| Physique shape | growth is per-axis, not uniform: girth leads, height trails, waist trails chest. At full scale chest is 3.80 wide vs a 2.60 waist and arms are 1.69 girth × 1.69 long | `PhysiqueConfig`, `MuscleController` |
| Vascularity | 9 vein cylinders on arms and chest, fading in from scale 1.35 to 2.0, client-side only | `PhysiqueConfig.VEINS`, `VeinController` |
| Scenery | decorative props are non-colliding and cleared 72 studs from any machine; kerbs no longer block | `scripts/build_gym.py` `_decorate`, `PROP_CLEARANCE` |
| Menu map | light "paper" board (232,234,238) with washed features and inked pins — the one panel that is not dark | `MenuController` `MAP_PAPER` |
| Muscle growth | per stat, not per total: Arms→upper/lower arms, Chest→UpperTorso, Back→UpperTorso (0.6), Core→LowerTorso, Legs→upper/lower legs. Shared parts take the largest contribution, never the product | `StatConfig.BodyScales`, `MuscleService:ComputeScales` |
| Machine visibility | the nearest 14 of 35 stations within 260 studs carry muscle-coloured outlines and floor rings | `StationHighlightController` |
| Dev commands | `/power`, `/stat`, `/tokens`, `/unlockall`, `/reset`, `/where`, `/help` — **Studio only**, gated on `RunService:IsStudio()` | `DevService.luau` |
| Quests | 3 — `DailyReps`, `DailyBounties` (daily), `FirstMillion` (one-shot) | `QuestService/Quests/` |
| Multiplier curve | `2^level`, capped at level 128 so the value stays finite | `Formulas.MultiplierValue` |
| Monetization | 4 product definitions — ImmortalPotion1Hour, ImmortalPotion1Day, VipGamepass, FastTravelGamepass. **Every `AssetId` is still `0`; nothing is purchasable** | `PurchaseService/Products/` |
| Persistence | ProfileStore, mock store in Studio by default (`USE_MOCK_IN_STUDIO`) | `DataService.luau` |
| Assets | no Toolbox models, meshes, or uploaded animations; poses come from `PoseConfig.luau` joint angles | repo policy |
| Sounds | `EffectsConfig.Sounds` ids are blank; the game is silent by design until filled | `EffectsConfig.luau` |

## Decisions recorded by this audit

1. **Training interruption is kill-only.** A single punch no longer knocks a player off
   their machine. The attacker must commit to a full kill to disrupt a set. This makes
   fights a decision rather than a drive-by tax on anyone standing at a machine, and it
   keeps the interruption cost proportional to the effort of causing it.
2. **Slam and Dash are deferred, not shipped.** Only Punch exists. They remain a wanted
   item for Phase 21's three-move kit (#122); until then no doc may imply they are in.
3. **Reputation, bounties, and safe zones are in.** Paid-peace fairness (whether an
   immortal player may still attack) is *not* settled here — it belongs to Phase 26.
4. **The five-location slice in #109 is superseded by the massive seven-tier world.** Each
   muscle keeps one recognizable primary exercise across seven environment-driven
   destinations. The city now spans roughly 3,000 × 2,700 studs; every muscle has one x1
   starter, five shuffled intermediate locations including one third-floor secret, and one
   x64 flight-only skyline platform. The ten extra exercise definitions remain future
   content rather than extra spawned station types.

## Known doc claims corrected by this audit

- `CLAUDE.md` / `AGENTS.md` rule 5 said a hit knocks the victim off the machine. Corrected.
- `CHECKLIST.md` #31 said "Punch, Slam, Dash ship first". Corrected to Punch only.
- `CHECKLIST.md` #32 described the hit-stagger interrupt. Corrected to kill-only.
- `README.md` said "60 numbered items across 10 phases". It is 178 items across 28 phases.
- `docs/PLAYTEST.md` described a 9/10-station gym, 5 poses, a 3-second stagger, and no
  flight or travel. Rewritten against this inventory.
